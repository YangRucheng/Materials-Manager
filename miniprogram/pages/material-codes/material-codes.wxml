<view class="page-shell material-codes-page">
  <view class="list-header"><view class="page-title">{{i18n.materialCodesTitle}}</view></view>
  <view wx:if="{{lastImportAtLabel}}" class="import-time">{{i18n.importTimeLabel}}：{{lastImportAtLabel}}</view>
  <view class="list-tools">
    <t-search
      value="{{keyword}}"
      placeholder="{{i18n.materialCodeSearchPlaceholder}}"
      shape="round"
      confirm-type="search"
      bind:change="onSearchChange"
    />
  </view>
  <view wx:if="{{!loading || items.length}}" class="result-count">{{resultCount}}</view>
  <view wx:if="{{loading && !items.length}}" class="state-panel">
    <t-loading theme="circular" size="48rpx" text="{{i18n.loadingMaterialCodes}}" />
  </view>
  <view wx:elif="{{!items.length}}" class="state-panel empty-panel">
    <t-empty icon="search" description="{{keyword ? i18n.materialCodesEmptyFiltered : i18n.materialCodesEmpty}}" />
  </view>
  <view wx:else class="code-list">
    <view
      wx:for="{{items}}"
      wx:key="id"
      class="code-card"
      hover-class="code-card-active"
      bind:tap="openDetail"
      data-index="{{index}}"
    >
      <view class="code-name">{{item.name || i18n.notSet}}</view>
      <view wx:if="{{item.model_spec}}" class="code-model">{{item.model_spec}}</view>
      <view class="card-divider"></view>
      <view class="code-value">{{item.material_code}}</view>
    </view>
  </view>
  <view wx:if="{{loadingMore}}" class="load-more">
    <t-loading theme="circular" size="36rpx" text="{{i18n.loadMore}}" />
  </view>
  <view wx:elif="{{items.length && !hasMore}}" class="load-finished">{{i18n.noMore}}</view>
  <t-toast id="material-codes-toast" />
  <button
    wx:if="{{backTopVisible}}"
    class="back-top-fab"
    aria-label="{{i18n.backToTop}}"
    bind:tap="scrollToTop"
  >
    <t-icon name="backtop" size="52rpx" color="#ffffff" />
  </button>

  <t-popup
    visible="{{detailVisible}}"
    placement="center"
    close-btn
    close-on-overlay-click
    bind:visible-change="onDetailVisibleChange"
  >
    <view class="detail-popup" wx:if="{{detailItem}}">
      <view class="detail-title">{{detailItem.name || i18n.notSet}}</view>
      <view class="detail-rows">
        <view class="detail-row">
          <view class="detail-label">{{i18n.modelSpecLabel}}</view>
          <view class="detail-value">{{detailItem.model_spec || '—'}}</view>
        </view>
        <view class="detail-row">
          <view class="detail-label">{{i18n.materialCodeLabel}}</view>
          <view class="detail-value">{{detailItem.material_code}}</view>
        </view>
        <view class="detail-row">
          <view class="detail-label">{{i18n.unitLabel}}</view>
          <view class="detail-value">{{detailItem.unit_name || '—'}}</view>
        </view>
      </view>
      <button class="copy-btn" bind:tap="copyMaterialCode">{{i18n.copyMaterialCode}}</button>
    </view>
  </t-popup>
</view>
